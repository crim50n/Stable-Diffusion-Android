package dev.minios.pdaiv1.domain.mocks

import android.net.Uri
import dev.minios.pdaiv1.domain.entity.MediaStoreInfo

val mockMediaStoreInfo = MediaStoreInfo(
    count = 5598,
    folderUri = Uri.EMPTY,
)
